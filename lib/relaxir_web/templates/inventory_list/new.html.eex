<h1>New Inventory list</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.inventory_list_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.inventory_list_path(@conn, :index) %></span>
